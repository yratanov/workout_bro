<%= turbo_stream.update("comment_modal", "") %>
<%= turbo_stream.update(
  "exercises",
  partial: "workout_routine_days/exercises",
  locals: {
    workout_routine_day: @workout_routine_day,
  },
) %>
