<%= turbo_stream.update(
  "reps_#{dom_id(@workout_set)}",
  partial: "list_with_form",
  locals: {
    workout_set: @workout_set,
    workout_rep: @workout_rep,
  },
) %>

<%= turbo_stream.update(
  "rest-timer-container",
  partial: "workout_reps/rest_timer",
  locals: {
    duration: @rest_duration,
  },
) %>
