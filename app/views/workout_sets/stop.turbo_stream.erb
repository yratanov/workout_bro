<%= turbo_stream.update(
  "new_workout_set",
  partial: "form",
  locals: {
    workout_set: ::WorkoutSet.new(workout: @workout),
  },
) %>

<%= turbo_stream.update(
  "workout_sets",
  partial: "workout_sets/list",
  locals: {
    workout: @workout,
  },
) %>
