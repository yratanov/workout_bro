<%= turbo_stream.update(
  "superset_exercises",
  partial: "superset_exercises/exercises",
  locals: {
    superset: @superset,
  },
) %>
<%= turbo_stream.update(
  "superset_exercise_count",
  t("supersets.show.exercise_count", count: @superset.superset_exercises.reload.count),
) %>
