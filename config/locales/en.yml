en:
  time:
    formats:
      short: "%d %b %H:%M"
      long: "%B %d, %Y %H:%M"
  date:
    formats:
      short: "%b %d"
      long: "%B %d, %Y"
  errors:
    messages:
      exercise_or_superset_required: "must have either an exercise or a superset"
      exercise_xor_superset: "cannot have both an exercise and a superset"
  activerecord:
    attributes:
      workout:
        distance: "Distance(m)"
        time_in_seconds: "Time (hh:mm:ss)"
    errors:
      models:
        workout_import:
          attributes:
            file:
              invalid_format: "must be a CSV file"
      template:
        header:
          one: "%{count} error prohibited this %{model} from being saved"
          other: "%{count} errors prohibited this %{model} from being saved"
