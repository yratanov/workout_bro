en:
  time:
    formats:
      short: "%d %b %H:%M"
      long: "%B %d, %Y %H:%M"
  date:
    formats:
      short: "%b %d"
      long: "%B %d, %Y"
  activerecord:
    attributes:
      workout:
        distance: "Distance(m)"
        time_in_seconds: "Time (hh:mm:ss)"
    errors:
      template:
        header:
          one: "%{count} error prohibited this %{model} from being saved"
          other: "%{count} errors prohibited this %{model} from being saved"
