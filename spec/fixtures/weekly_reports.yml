# == Schema Information
#
# Table name: weekly_reports
# Database name: primary
#
#  id            :integer          not null, primary key
#  ai_summary    :text
#  error_message :text
#  status        :integer          default("pending"), not null
#  viewed_at     :datetime
#  week_start    :date             not null
#  created_at    :datetime         not null
#  updated_at    :datetime         not null
#  user_id       :integer          not null
#
# Indexes
#
#  index_weekly_reports_on_user_id                 (user_id)
#  index_weekly_reports_on_user_id_and_week_start  (user_id,week_start) UNIQUE
#
# Foreign Keys
#
#  user_id  (user_id => users.id)
#
johns_completed_report:
  user: john
  week_start: <%= Date.current.beginning_of_week(:monday) - 7.days %>
  ai_summary: "Great training week! You hit all your targets."
  status: 1

johns_pending_report:
  user: john
  week_start: <%= Date.current.beginning_of_week(:monday) %>
  status: 0

janes_report:
  user: jane
  week_start: <%= Date.current.beginning_of_week(:monday) - 7.days %>
  ai_summary: "Good progress this week."
  status: 1
